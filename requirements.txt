# Only packages NOT already preinstalled (and CUDA/Python ABI-matched) in
# Colab. Deliberately unpinned and excludes torch/torchvision/numpy/pandas/
# pillow/scikit-learn/tqdm -- those are already present and working in Colab;
# pinning them forces pip to reinstall/rebuild compiled extensions against a
# possibly-mismatched build, which is what broke Pillow's _imaging extension
# last run. Restart the Colab runtime after installing these, before running
# any cell that imports them.
timm
facenet-pytorch
datasets
kaggle
gradio
