- `notes.md` - guidelines and to-dos for the user
- `docs/README.md` - index of all docs below, with the lifecycle convention (decisions / reference / research / investigations) explained
- `docs/decisions/0001-architecture-decisions.md` - requirements and architecture decisions with reasoning, incl. the finalized dataset choice. **Status: partially superseded by 0002** (the CLIP-ViT/DINOv2 rejection and the Dataset section); superseded passages are marked inline, not rewritten
- `docs/decisions/0004-adaptation-hypothesis-demo-build.md` - **current decision (2026-08-02).** Changes the claim from "it generalizes" to **"adaptation costs N images"**, with images-per-new-generator as the deliverable — the one framing that survives the fixed live-test scenario, since a wrong 0-shot answer becomes the pre-stated left endpoint of a curve rather than the end of the demo. Corrects `0003` §5: the multi-generator data was already in COCO_AI, discarded by one line (`data/download.py:68`). Replaces resize-and-crop with **fixed-N (16) native patches + one whole-image view**, because the resize ratio differs by class (~2.1× vs ~4.6×) and manufactures a label-correlated confound. Drops DINOv2 and PE-Core outright; adds Head B (generator ID) + Mahalanobis OOD gate, evidence-level card fusion with quarantined provenance, and seven pre-registered experiments. **Supersedes `0003` §4.3, §5, most of §6**
- `docs/decisions/0003-frozen-probe-demo-build.md` - **partially superseded by 0004** (§4.3, §5, most of §6). Closes the research chapter on the v1 checkpoint and retires v1 from the Monday-demo slot. Builds a frozen CLIP ViT-L/14 + logistic-regression probe, binary real vs. AI-generated, on **whole images** (face cropping dropped). Records that 0002 §9's falsification gate is spent rather than passed, and that the binding constraint is **data, not modelling** — COCO_AI is DALL·E-3-only, so ≥2 training generators is a requirement of the build. §6 lists what is deliberately not built
- `docs/decisions/0002-frozen-backbone-generalization.md` - **Status: partially superseded by 0003** (§9 and the conditional status). Full arc from lit survey -> original decision -> what the eval showed -> new facts -> decision: adopt frozen foundation-model features + a linear probe as an *additional* generalization path alongside the 3-branch model. Status: Accepted (conditional) on the gating experiment in its §9. §6.6/§8.5/§10/§11 carry inline amendments dated 2026-07-31 from the claim-verification pass, and §10/§11 carry a further 2026-08-01 amendment from the calibration pass that **withdraws** §10's calibration/abstention conclusions. Also records why `edited` is deferred and why the family (c) rejection in the research doc was a framing error
- `docs/reference/data_download.md` - instructions (no code) for downloading + face-filtering the 3 class datasets; code in `data/download.py` and `data/face_filter.py`
- `docs/reference/model_code.md` - instructions (no code) for the 3-branch fusion model, training, and eval; code in `model/*.py` + `forgery_classifier.ipynb`
- `docs/reference/resolution_swap_probe.md` - spec for the `rgb`-direct probe (implemented as `model/resolution_swap_probe.py`); kept in `reference/` because the 2026-07-27 investigation links to it and investigations are immutable
- `docs/reference/2026-08-02-session-findings.md` - the artifact taxonomy: "the artifact" is three things - (A) generator-specific spectral fingerprint, (B) decode-stage statistics, (C) container/corpus artifacts - which behave differently and were being conflated. Establishes (A) as genuine-but-non-transferring, (B) as the causally-established transfer hope, (C) as pure shortcut priced at ~11 points. Records Wang et al.'s actual probabilistic augmentation protocol and **four corrections**, incl. §5.1 "transformer generators have no decode stage" (false - they decode through a VQ-VAE tokenizer, which is what licenses (B) as a shared target)
- `docs/reference/2026-08-02-plan-a-verification.md` - verifies the buy-transformer-data plan. Legitimate engineering, does not prove the hypothesis (it deletes the generalization gap rather than closing it). Finds that **SynthID is engineered to survive the plan's own control test**, so the stated safeguard is blind to the most likely confound; proposes codec A/B and re-render as discriminating replacements
- `docs/reference/2026-08-02-plan-b-verification.md` - verifies the augmentation-forced-invariance plan. **Its §1 and §4 are partly superseded** - see `2026-08-02-bottlenecks.md` §2.4 and §5: the UniFD 0.00% result rests on a since-withdrawn paper, and the 41.8-66.9% real-accuracy figure is misattributed to SSAFE (it belongs to AIGI-Holmes-trained baselines). §3's encoder ablation and §5's NTIRE reading are accurate and load-bearing
- `docs/reference/2026-08-02-plan-c-source-verification.md` - verifies the threshold-correction plan against Yang et al. at primary source. Closes the calibration-set-size open item (100 default, stable to 10) and **downgrades the plan's premise**: "misaligned thresholds rather than lost separability" is the paper's *attribution*, not a measurement - it reports accuracy only, no AUC, no ranking analysis. Makes our AUC run a first measurement rather than a replication. Also records DailyBench's withdrawal
- `docs/reference/2026-08-02-ssafe-primary-read.md` - **second 08-02 pass; amends the three above.** SSAFE read at the paper body rather than the abstract, resolving the contradiction between plan-B (body read) and plan-C (abstract only) in plan-B's favour. Records the full training/test composition, the MMD + hierarchical-clustering + Farthest-Point-Sampling curation method, **Table 5 (4M images -> 29.7% real TNR vs 10K curated -> 98.3%: composition beats scale by 400x)**, Table 12's encoder ablation, and cross-paradigm transfer to Janus/LlamaGen/GPT-Image-1/Nano-Banana from diffusion+GAN training only. Also records what SSAFE does *not* contain - no mechanism analysis, no compression/format/resolution controls, no data release, and an **uncontrolled real class** (§2.8). Plus the Perception Encoder's intermediate-layer finding, why a frozen backbone is a *ceiling* rather than a hyperparameter (§4), and six corrections (§5) incl. plan-B §4's misattribution and plan-B §1's reliance on the withdrawn DailyBench
- `docs/reference/2026-08-02-dataset-alignment.md` - **third 08-02 pass; answers "how do you build a real:fake distribution that differs only by synthesis."** Records a reconstruction-pairing proposal made and **falsified** the same session (it is AlignedForensics ICLR 2025 rediscovered, and collapses outside the SD-VAE family - 25.87% on FLUX, 3.6% on DDA-COCO/FLUX, 53.9% avg on ForenSynths), with the mechanism failure measured by DRCT: VAE reconstructions cluster with **real** in t-SNE and Fourier space while generations do not. Corrects AEROBLADE's 0.992 mAP as a **matched-decoder** number (cross-decoder 0.543-0.623) and corrects two misdescriptions of DRCT. Introduces **DDA (arXiv:2505.14359)**, which diagnoses single-sided alignment as leaving a JPEG/high-frequency confound and fixes it by format-matching the reconstruction (FLUX 3.6 -> 50.2; Infinity 74.0 -> 97.8) - the paper most directly about this question, body not yet read. §7 records the easy-task trap; §10 records six corrections to the session's own reasoning
- `docs/notes/2026-08-02-bottlenecks.md` - **current inventory.** All bottlenecks ranked by cost-to-clear over consequence, across data / model / evaluation / evidence-base / time. Amends `0003` §5's "the binding constraint is data" with the half it missed: the corpus occupies a resolution band (270-480px) and a single architecture family (all six COCO_AI columns are latent diffusion) that neither the recipe being copied nor the demo distribution occupies. §2.4 records why the obvious fix to the real class — downloading modern stock/phone reals — is a **trap** here: SSAFE's modern reals are unpaired and its real class is uncontrolled (no source-characteristic discussion, no isolating ablation, no synthetic-resolution stats), so adding unpaired high-res reals to our low-res fakes would make "modern high-res photo = real" the cheapest hypothesis and fail the demo by construction. §7 records what is *not* a bottleneck, incl. that COCO_AI's 1:1 pairing is now the most valuable property our data has; §8 gives a clearing order with a standing pairing constraint; §9 lists five open measurements
- `docs/notes/2026-08-02-build-plan.md` - execution plan for `0004`. Hard 7-hour cap, cut order fixed in advance (S1-S5 never cut), eight code-asserted preprocessing invariants, and the note that S7 (fusion/verdict) doubles as P1 drill #1
- `docs/research/2026-08-02-patch-inference-and-encoder-choice.md` - **fourth 08-02 pass; the evidence behind `0004` §6-§7.** Finds a **fourth resolution problem** missing from `bottlenecks.md` §2.1: the downscale *ratio* differs by class, so the resize step manufactures a label-correlated artifact - §2.1(b)'s bug surviving into a new model. §5 records the **bag-size leak** (patch count encodes the label; leaks through max, top-k *and* mean via variance) as a correctness requirement. §7 gives the mechanism that settles the encoder - DINOv2's multi-crop objective trains crops to embed alike, making it structurally worst under patching - and overrides §3.1's two-arm recommendation because our run cannot resolve the 7B-scale confound it was proposed to resolve. Confirms Chai et al. (ECCV 2020) but marks the load-bearing-sounding claim `[unverified]` on purpose. §9 records four corrections to the session's own reasoning
- `docs/research/2026-07-31-production-deployment.md` - what changes when moving from a research detector to a deployed system: base-rate math, the four layers (provenance / classifier / abstention / review), generator acquisition as the real asset, abstention as the only label-free monitoring signal, post-processing robustness as a spec, label feedback without customer-data rights, and which research principles survive/invert/die. Decision input for a future `0003`, not a decision
- `docs/research/2026-07-31-literature-triage.md` - method for triaging a contradictory literature: three causes of apparent disagreement, claim half-life tiers 0-4, a 90-second filter, and the claims-ledger / decisions-log / kill-list trio. Tracks claims rather than papers
- `docs/research/2026-08-01-calibration-and-thresholds.md` - **reverses** the 07-31 conclusion that calibration cannot address the cross-generator failure: Yang et al. (AAAI 2026) attributes it substantially to *misaligned decision thresholds rather than lost separability*, correctable post-hoc with a scalar logit adjustment, backbone frozen, label-free variant available. Records the prerequisite chain (a score is not a probability until measured; base-rate shift changes the conclusion without changing the evidence; prior correction *requires* a calibrated model) and where the theory stops (an unseen generator changes `p(x|fake)`, violating the label-shift assumption). Uses a `[second-hand]` tag for numbers not re-checked against primary sources. Amends 0002 §10, §11
- `docs/research/2026-07-31-claim-verification.md` - narrow audit of the mechanistic claims behind 0002, with per-claim sources and status. Confirms the VAE-decode-artifact mechanism and the confident-failure-toward-`real` property; **corrects** the "diffusion family shares a decoder, so detection transfers within it" claim (VAE configs diverge; Flux Dev is at 21%). Amends 0002 §6.6, §8.5, §10, §11. **Status: partially superseded** - §1.2's calibration conclusion and §5's threshold/separability open item are overtaken by the 2026-08-01 pass; findings left unedited per the `research/` convention, with a forward-pointing header
- `docs/research/deepfake_detection_research.md` - the 2026-07-23 SOTA survey. **Status: historical, recommendations superseded** - carries an errata header; survey content still useful, §1's recommendations replaced by 0001 (dataset) and 0002 (architecture)
- `docs/investigations/2026-07-26-upscale-artifact.md` - investigation into severe blur seen in `docs/screenshots/demo_*.png` across all 3 classes: traces it to MTCNN's fixed bilinear crop-resize (`data/face_filter.py`/`model/demo.py`) upscaling small detected face boxes ~10-12x median across real/edited/deepfake alike, and the resulting (revised) shortcut-learning hypothesis with sources
- `docs/investigations/2026-07-27-fft-srm-template-swap-probe.md` - first counterfactual probe (`model/counterfactual_probe.py`); rules out a targeted per-class fft/srm-template shortcut, but records its own scope gap (never varies `rgb`)
- `docs/investigations/2026-07-27-resolution-swap-probe.md` - the `rgb`-direct follow-up (`model/resolution_swap_probe.py`); rules out a general resolution-magnitude shortcut on 2 of 3 pairs, replicated across a >30% perturbation-size swing. One small `real->edited` residual left open
- `docs/notes/linear_probe_loss_functions.md` - background note on logistic regression vs. Ridge classifier as linear probes (relevant to 0002 §9); code in `docs/notes/loss_boundary_demo.py`, plot in `docs/notes/loss_boundary_demo.png`
- `docs/screenshots/*.png` - live-demo screenshots (one per class) referenced by the README and the upscale investigation doc
- `config.py` - shared config for data and model paths
- `data/download.py`, `data/face_filter.py` - COCO_AI/CASIA download and face-filter pipeline
- `model/branches.py`, `model/fusion.py`, `model/dataset.py`, `model/train.py`, `model/eval.py` - 3-branch fusion model, dataset, training, and eval code
- `model/demo.py` - standalone Gradio demo (face-crop + 3-branch prediction + Grad-CAM overlay) for arbitrary uploaded images
- `forgery_classifier.ipynb` / `forgery_classifier_final.ipynb` - Colab notebook wiring together Data/Train/Eval sections; `_final` is the executed run with outputs committed
- `requirements.txt` - frozen environment dependencies